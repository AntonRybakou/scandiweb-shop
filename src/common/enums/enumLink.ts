export enum LINK {
  LOGIN = '/login',
  EMAIL = '/email',
  PROFILE = '/profile',
  RECOVER = '/recovery',
  PASSWORD = '/password/:token',
  FOUND404 = '/404',
  REGISTER = '/register',
  TEST = '/test',
}
